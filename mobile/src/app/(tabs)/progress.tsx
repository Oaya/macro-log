import { Text, View } from "react-native";

export default function Progress() {
	return (
		<View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
			<Text style={{ fontSize: 20 }}>Log Food (coming soon)</Text>
		</View>
	);
}
